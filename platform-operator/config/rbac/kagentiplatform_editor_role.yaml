# This rule is not used by the project kagenti-platform-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the kagenti.dev.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: kagenti-platform-operator
    app.kubernetes.io/managed-by: kustomize
  name: kagentiplatform-editor-role
rules:
- apiGroups:
  - kagenti.dev
  resources:
  - kagentiplatforms
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - kagenti.dev
  resources:
  - kagentiplatforms/status
  verbs:
  - get

# This rule is not used by the project kagenti-platform-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to kagenti.dev resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: kagenti-platform-operator
    app.kubernetes.io/managed-by: kustomize
  name: kagentiplatform-viewer-role
rules:
- apiGroups:
  - kagenti.dev
  resources:
  - kagentiplatforms
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - kagenti.dev
  resources:
  - kagentiplatforms/status
  verbs:
  - get
